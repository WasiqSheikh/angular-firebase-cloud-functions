import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'angular-firebase-cloud-functions',
  webDir: 'www'
};

export default config;
